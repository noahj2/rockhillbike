{% extends 'base.html.twig' %}

{% block body %}
    <h1>WellnessTip</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wellnessTip.id }}</td>
            </tr>
            <tr>
                <th>Tip</th>
                <td>{{ wellnessTip.tip }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_wellnesstip_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_wellnesstip_edit', { 'id': wellnessTip.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
