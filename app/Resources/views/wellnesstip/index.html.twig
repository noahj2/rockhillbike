{% extends 'adminBase.html.twig' %}

{% block title %}Wellness Tip Index{% endblock %}

{% block body %}
    
    <h4>Wellness Tips List</h4><br>
    
    {% for wellnessTip in wellnessTips %}
        <label>Tip:</label> {{ wellnessTip.tip }}
        <div>
            <a href="{{ path('admin_wellnesstip_edit', { 'id': wellnessTip.id }) }}">Edit</a>
        </div><br>
    {% endfor %}
    
    <div>
        <ul>
            <li>
                <a href="{{ path('admin_wellnesstip_new') }}">Create New Entry</a>
            </li>
        </ul>
    </div>
{% endblock %}
