{% extends 'adminBase.html.twig' %}

{% block title %}Comment Creation{% endblock %}

{% block body %}

    <h4>Comment Creation</h4><br>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Submit" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_comment_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
