{% extends 'base.html.twig' %}

{% block title %}Front Comment Creation{% endblock %}

{% block body %}

    <h4>Leave a Comment</h4><br>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('comment_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}