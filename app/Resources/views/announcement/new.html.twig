{% extends 'adminBase.html.twig' %}

{% block title %}Announcement Creation{% endblock %}

{% block body %}
    <h1>Announcement Creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('announcement_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
